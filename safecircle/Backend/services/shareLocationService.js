
// Need to implement the logic to share the location via method (e.g., sending to a server, SMS, etc.)
export const shareLocation = (location, contacts) => {
    //  logic to share location
    console.log('Sharing location', location, 'with contacts', contacts);
  };
  